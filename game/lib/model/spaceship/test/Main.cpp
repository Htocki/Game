#include "gtest/gtest.h"

int main(int argc, char** argv) {
  ::testing::InitGoogleTest(&argc, argv);
  return RUN_ALL_TESTS();
}

TEST(Spaceship, DefaultConstructor) {
  // Arrage
  // Act
  // Assert
}

TEST(Spaceship, SetSpeed) {
  // Arrage
  // Act
  // Assert
}

TEST(Spaceship, GetSpeed) {
  // Arrage
  // Act
  // Assert
}

TEST(Spaceship, MoveLeft) {
  // Arrage
  // Act
  // Assert
}

TEST(Spaceship, MoveRight) {
  // Arrage
  // Act
  // Assert
}

TEST(Spaceship, MoveUp) {
  // Arrage
  // Act
  // Assert
}

TEST(Spaceship, MoveDown) {
  // Arrage
  // Act
  // Assert
}
