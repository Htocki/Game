#include "gtest/gtest.h"

int main(int argc, char** argv) {
  ::testing::InitGoogleTest(&argc, argv);
  return RUN_ALL_TESTS();
}

TEST(EventHandler, HandleEvent) {
  // Arrange
  // Act
  // Assert
}

TEST(EventHandlerKeeper, Attach) {
  // Arrange
  // Act
  // Assert
}

TEST(EventHandlerKeeper, Detach) {
  // Arrange
  // Act
  // Assert
}

TEST(EventHandlerKeeper, NotifyEventHandlers) {
  // Arrange
  // Act
  // Assert
}
