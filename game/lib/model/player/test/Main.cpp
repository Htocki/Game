#include "gtest/gtest.h"

int main(int argc, char** argv) {
  ::testing::InitGoogleTest(&argc, argv);
  return RUN_ALL_TESTS();
}

TEST(Player, DefaulConstructor) {
  // Arrange
  // Act
  // Assert
}

TEST(Player, SetName) {
  // Arrange
  // Act
  // Assert
}

TEST(Player, GetName) {
  // Arrange
  // Act
  // Assert
}

TEST(Player, GetSpaceship) {
  // Arrange
  // Act
  // Assert
}

TEST(Player, HandleEvent) {
  // Arrange
  // Act
  // Assert
}
