#include "gtest/gtest.h"

int main(int argc, char** argv) {
  ::testing::InitGoogleTest(&argc, argv);
  return RUN_ALL_TESTS();
}

TEST (Spaceship, PositionableConstructor) {
  // Arrange
  // Act
  // Assert
}

TEST (Spaceship, DrawOnWindow) {
  // Arrange
  // Act
  // Assert
}

TEST (Spaceship, Update) {
  // Arrange
  // Act
  // Assert
}
