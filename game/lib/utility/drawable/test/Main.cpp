#include "gtest/gtest.h"

int main(int argc, char** argv) {
  ::testing::InitGoogleTest(&argc, argv);
  return RUN_ALL_TESTS();
}

TEST(Drawable, DrawOnWindow) {
  // Arrange
  // Act
  // Assert
}

TEST(Drawning, Attach) {
  // Arrage
  // Act
  // Assert
}

TEST(Drawning, Detach) {
  // Arrange
  // Act
  // Assert
}

TEST(Drawning, NotifyDrawables) {
  // Arrange
  // Act
  // Assert
}
